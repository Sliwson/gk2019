R""(
	#version 460 core

	out vec4 fragmentColor;

	in vec3 outColor;
	in vec2 textureCoordinates;

	uniform sampler2D shaderTexture;

	void main()
	{
		fragmentColor = texture(shaderTexture, textureCoordinates) * vec4(outColor, 1.0); 
	} 
)""